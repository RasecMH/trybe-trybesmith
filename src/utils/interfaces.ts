export interface Product {
  id: number;
  name: string;
  amount: string;
}

export interface ErrorObj {
  status: number;
  message: string;
}

export interface User {
  id: number;
  username: string;
  classe: string;
  level: number;
}